stages: [dispatch]

default:
  tags:
    - local
infra:
  stage: dispatch
  trigger:
    include:
      - local: "ci/.gitlab-infra.yml"
    strategy: depend
  rules:
    - when: manual

platform:
  stage: dispatch
  trigger:
    include:
      - local: "ci/.gitlab-platform.yml"
    strategy: depend
  rules:
    - when: manual

Release:
  stage: dispatch
  trigger:
    include:
      - local: "ci/.gitlab-relese.yml"
    strategy: depend
  rules:
    - when: manual